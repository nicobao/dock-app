import {AccountAdvancedOptions} from '../../../components/AccountAdvancedOptions';
import React from 'react';

export function DIDAdvancedOptions({onChange, form}) {
  return <AccountAdvancedOptions onChange={onChange} form={form} />;
}
